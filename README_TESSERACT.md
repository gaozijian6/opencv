# OCR 识别工具使用说明（Tesseract版本）

## 问题说明

经过详细调试发现：
- **macOS 26.1** 系统的 Python 3.9.6 中的 **tkinter** 库与系统不兼容
- 无论使用虚拟环境还是系统 Python，GUI 都无法正常启动
- **PyTorch** 所有版本（1.12.1, 1.13.1, 2.0.1, 2.8.0）都无法在当前系统运行

## 解决方案

使用 **命令行版本** + **Tesseract OCR**（不依赖 GUI，不需要 PyTorch）

---

## 🚀 使用方法

### 1. 命令行版本（推荐）

```bash
# 识别单张图片
/usr/bin/python3 ocr_cli_tesseract.py image1.jpg

# 或直接运行
./ocr_cli_tesseract.py image1.jpg
```

**示例输出：**
```
📸 已加载图片: image1.jpg
   尺寸: 1179 x 2556
🔍 正在识别...

==================================================
✅ 识别结果: 1  2  3  4  5
   找到 5 个数字
==================================================
```

### 2. 原有的 index.py（如果之前能用）

```bash
/usr/bin/python3 index.py
```

---

## 📦 已安装工具

### 系统级别
- **Tesseract OCR 5.5.2**（通过 Homebrew 安装）
- **pytesseract**（Python 接口）

### 虚拟环境（opencv_env）
- PyTorch 相关库已移除（兼容性问题）
- 仅保留：opencv-python, numpy, pytesseract

---

## 文件说明

| 文件名 | 说明 | 状态 |
|--------|------|------|
| `ocr_cli_tesseract.py` | 命令行版本（Tesseract） | ✅ 可用 |
| `index.py` | 原有版本（EasyOCR） | ⚠️  依赖PyTorch |
| `ocr_gui_tesseract.py` | GUI版本（Tesseract） | ❌ tkinter不兼容 |
| `ocr_gui_simple.py` | GUI版本（EasyOCR） | ❌ PyTorch不兼容 |
| `ocr_gui_nolib.py` | GUI测试版 | ❌ tkinter不兼容 |

---

## 📋 已知限制

1. **GUI无法使用**：macOS 26.1 的 tkinter 兼容性问题
2. **EasyOCR不可用**：PyTorch 无法在当前系统运行
3. **识别准确率**：Tesseract 对小尺寸数字的识别可能不如 EasyOCR

---

## 💡 建议

### 短期方案
使用命令行版本进行识别

### 长期方案（如需GUI）
1. **降级 macOS** 到 Big Sur 或 Monterey（如果可行）
2. 使用 **PyQt/PySide** 替代 tkinter 创建 GUI
3. 等待 Apple 或 Python 社区修复 tkinter 兼容性问题

---

## 🔧 故障排查

### 如果 Tesseract 识别效果不好

尝试调整图片预处理参数：

```python
# 在 ocr_cli_tesseract.py 中修改
resized = cv2.resize(image, (120, 120))  # 尝试更大的尺寸
```

### 检查 Tesseract 是否正常

```bash
# 查看版本
tesseract --version

# 测试识别
tesseract image1.jpg output -c tessedit_char_whitelist=123456789
cat output.txt
```

---

## 📞 技术支持

如有问题，请提供：
1. macOS 版本：`sw_vers`
2. Python 版本：`/usr/bin/python3 --version`
3. Tesseract 版本：`tesseract --version`
4. 错误信息截图
